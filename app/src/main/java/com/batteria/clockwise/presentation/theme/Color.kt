package com.batteria.clockwise.presentation.theme

import androidx.compose.ui.graphics.Color

/**
 * ClockWise v3 — Bluey-inspired palette
 * Reference: Australian "Bluey" TV show
 */
object BlueyPalette {
    // Backgrounds — cream, eye-friendly (not pure white)
    val Bg          = Color(0xFFFFF8E7)   // warm cream
    val BgElevated  = Color(0xFFFFFDF5)   // lighter cream for cards
    val BgDeep      = Color(0xFFF5E8C8)   // warm tan for depth

    // Bluey blues (heeler family)
    val Bluey       = Color(0xFF4FB3FF)   // Bluey's bright blue
    val BlueyDeep   = Color(0xFF1E78D9)   // deeper blue accent
    val BlueySoft   = Color(0xFFB3E0FF)   // powder blue

    // Bingo orange (Bluey's sister)
    val Bingo       = Color(0xFFFF9542)
    val BingoSoft   = Color(0xFFFFD4A8)

    // Bandit royal blue + Chilli red-orange
    val Bandit      = Color(0xFF2563EB)   // dad
    val Chilli      = Color(0xFFE74C3C)   // mum

    // Cartoon accents
    val Mint        = Color(0xFF4ED8A0)
    val Sun         = Color(0xFFFFDA6E)
    val Rose        = Color(0xFFFF8FBE)

    // Text / outlines
    val Ink         = Color(0xFF1E3A52)   // warm navy
    val InkSoft     = Color(0xFF4A6B8A)
    val Outline     = Color(0xFFC8B89A)   // warm beige
}
